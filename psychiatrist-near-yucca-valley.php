<?php
/**
 * Location page entry point: /psychiatrist-near-yucca-valley
 *
 * Area pages live at the site root so the plain extensionless rule serves
 * them, with no dependency on a rewrite rule. The page itself still lives in
 * areas-we-serve/ and resolves its own includes relative to that folder.
 */
require __DIR__ . '/areas-we-serve/location-psychiatrist-near-yucca-valley.php';
